name: pulumi-azure-serverless-example
runtime: dotnet
description: Azure functions backed by CosmosDB
template:
  config:
    azure:environment:
      description: The Azure environment to use (`public`, `usgovernment`, `german`, `china`)
      default: public